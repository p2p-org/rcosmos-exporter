general:
  network: pell-testnet
  mode: network
  chain_id: cometbft
  metrics:
    address: 0.0.0.0
    port: 9100
    path: /metrics
  alerting:
    validators:
      - 45FD155835C6709A0A454342E5B9EA7A8982DCC8
  nodes:
    rpc:
      - name: p2p
        url: https://rpc.pell-testnet.tm.p2p.org
        healthEndpoint: /health
    lcd:
      - name: p2p
        url: https://api.pell-testnet.tm.p2p.org
        healthEndpoint: /cosmos/base/node/v1beta1/status

node:
  client: p2p
  tendermint:
    nodeInfo:
      enabled: true
      interval: 30
  cometbft:
    status:
      enabled: true
      interval: 30

network:
  cometbft:
    validators:
      enabled: true
      interval: 10
    block:
      enabled: true
      interval: 10
      window: 500
      tx:
        enabled: true
      uptime:
        persistence: true
      
  tendermint:
    bank:
      addresses: []
      enabled: false
      interval: 30
    distribution:
      enabled: true
      interval: 30
    gov:
      enabled: false
      interval: 30
    staking:
      enabled: true
      interval: 30
    slashing:
      enabled: true
      interval: 30
    upgrade:
      enabled: true
      interval: 30
  
  mezo:
    poa:
      enabled: false
      interval: 30
  
  babylon:
    bls:
      enabled: false
      interval: 30
  
  lombard:
    ledger:
      addresses: []
      enabled: false
      interval: 30
  
  namada:
    account:
      addresses: []
      enabled: false
      interval: 30
    pos:
      enabled: false
      interval: 30
  
  coredao:
    block:
      enabled: false
      interval: 30
      window: 500
    validator:
      enabled: false
      interval: 30
