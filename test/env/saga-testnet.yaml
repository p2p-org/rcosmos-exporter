general:
  network: saga-testnet
  chain_id: cometbft
  mode: network
  metrics:
    address: 0.0.0.0
    port: 9100
    path: /metrics
  alerting:
    validators:
      - C22C4618C5AFF753529BA40BCAE6EC03726FC3B7
  nodes:
    rpc:
      - name: nodestake
        url: https://rpc-t.saga.nodestake.org/
        healthEndpoint: /status
      - name: p2p
        url: http://saga-testnet-p2p-sentry.tm.p2p.world
        healthEndpoint: /status
    lcd:
      - name: nodestake
        url: https://api-t.saga.nodestake.org
        healthEndpoint: /cosmos/base/node/v1beta1/status
      - name: p2p
        url: http://saga-testnet-p2p-sentry-rest.tm.p2p.world
        healthEndpoint: /cosmos/base/node/v1beta1/status

node:
  client: 
  tendermint:
    nodeInfo:
      enabled: true
      interval: 30
  cometbft:
    status:
      enabled: true
      interval: 30

network:
  cometbft:
    validators:
      enabled: true
      interval: 30
    block:
      enabled: true
      interval: 30
      window: 500
      tx:
        enabled: true
      uptime:
        persistence: true

  tendermint:
    bank:
      addresses: []
      enabled: false
      interval: 30
    distribution:
      enabled: true
      interval: 30
    gov:
      enabled: true
      interval: 30
    staking:
      enabled: true
      interval: 30
      validators: true
      delegations: true
      commissions: true
      pool: true
      params: true
    slashing:
      enabled: true
      interval: 30
    upgrade:
      enabled: true
      interval: 30

  mezo:
    poa:
      enabled: false
      interval: 30

  babylon:
    bls:
      enabled: false
      interval: 30

  lombard:
    ledger:
      addresses: []
      enabled: false
      interval: 30

  namada:
    account:
      addresses: []
      enabled: false
      interval: 30
    pos:
      enabled: false
      interval: 30

  coredao:
    block:
      enabled: false
      interval: 30
      window: 500
    validator:
      enabled: false
      interval: 30
      api:
        enabled: false
        url: 
        api_key: 
        cache_duration_seconds: 300
    staking:
      enabled: false
      interval: 30

  sei:
    validators:
      enabled: false
      interval: 10
    block:
      enabled: false
      interval: 10
      window: 500
      tx:
        enabled: false
      uptime:
        persistence: false
